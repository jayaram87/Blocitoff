<div class='form group'>
<%= form_for [user, item] do |f| %>
    <%= f.label :name %>
    <%= f.text_area :name, class: 'form-control', placeholder: "Enter the item to do" %>
    <div class="form-group">
        <%= f.submit "Create Item", class: 'btn btn-success' %>
    </div>
<% end %>
</div>